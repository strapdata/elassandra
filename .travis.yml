sudo: required
language: java
env:
  global:
  - DOCKER_IMAGE=strapdata/elassandra-builder
notifications:
  email: true
services:
- docker
install: true
script:
  - docker run -v ${PWD}:/src ${DOCKER_IMAGE}
